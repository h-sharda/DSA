// Unsolved

#include <bits/stdc++.h>
using namespace std;

void fast_io() {
    ios::sync_with_stdio(0);
    cin.tie(0);
    cout.tie(0);
}

int main() {
    fast_io();
    int t;
    cin >> t;
    while (t--) {
        int n;
        cin >> n;

        vector<int> nums(n, 0);
        for (int i = 0; i < n; i++) {
            cin >> nums[i];
        }

        vector<int> v(11, 0);
        int ans = 0;

        for (int i =0; i < n; i++) {
            
        }


        cout << ans << endl;
        
    }

    return 0;
}